# Custom Report Instances Table

A reusable OPAL report instances table for displaying generated report runs with sorting, pagination and horizontal scrolling.

The consuming application owns API mapping and should pass rows that match `ICustomReportInstancesTableData`.
